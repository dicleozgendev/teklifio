name: Quality checks

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      NEXT_PUBLIC_APP_ENV: development
      NEXT_PUBLIC_DEMO_MODE: "true"
      NEXT_PUBLIC_SEED_DEMO_DATA: "false"
    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Set up Java for Firebase Emulator
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "21"

      - name: Run unit and security tests
        run: npm run test:unit

      - name: Run Firestore rules bypass tests
        run: npx firebase-tools@15.11.0 emulators:exec --only firestore --project demo-teklifio-security "node --test tests/firestore-rules.emulator.mjs"

      - name: Run browser tests
        run: npm run test:e2e

      - name: Run lint
        run: npm run lint

      - name: Run production build
        run: npm run build

      - name: Upload browser test artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-artifacts
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 7
